<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('includes/head', { title: 'All Posts' }) %>
  <link rel="stylesheet" href="/styles/posts.css">
</head>
<body>
    <%- include('includes/header') %>

    <main id="all-repairs">
        <h1>예약 내역</h1>
    <% if(!repairs || repairs.length === 0) { %>
        <p><br>예약 내역이 없습니다.</p>
    <% } else { %>
        <ol id="repairs-list">
            <% for(const repair of repairs) { %>
                <li>
                    <%- include('includes/repair-content', {repair:repair}) %>
                </li>
            <% } %>
        </ol>
    <% } %>
    </main>
</body>
</html>