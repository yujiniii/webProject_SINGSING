<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('includes/head', { title: 'All Repairs' }) %>
  <link rel="stylesheet" href="/styles/posts.css">
</head>
<body>
    <%- include('includes/header') %>

    <main id="all-posts">
        <h1>예약 내역</h1>

        <% if(!select_repairman || select_repairman.length === 0) { %>
            <p><br>예약 내역이 없습니다.</p>
        <% } else { %>
            <ol id="posts-list">
                <% for(const repair of select_repairman) { %>
                    <li>
                        <%- include('includes/repair-content', {repair:repair}) %>
                    </li>
                <% } %>
            </ol>
        <% } %>


    
    </main>
</body>
</html>